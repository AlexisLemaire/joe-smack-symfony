{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block stylesheets %} 
    <link href="{{ asset('css/forms.css') }}" rel="stylesheet">
{% endblock %}

{% block body %}
<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div> <br />
    {% endif %}

    <h1 class="h3 mb-3 font-weight-normal">Connexion</h1>
    <strong>Vous serez redirigé vers cette page si votre compte n'est pas vérifié</strong> <br />
    <strong><a href="{{ path('email_resend') }}">Email non reçu ?</a></strong> | 
    <strong><a href="{{ path('forgot_password') }}">Mot de passe oublié ?</a></strong>
    <br /><br />
    <label for="inputEmail">Email</label>
    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
    <label for="inputPassword">Password</label>
    <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

    <div class="checkbox mb-3">
        <label>
            <input type="checkbox" name="_remember_me"> Remember me
        </label>
    </div>

    <button class="btn btn-lg btn-primary w-100" type="submit"> Sign in </button> <br />

    <strong><a href="{{ path('app_register') }}">Vous n'avez pas de compte ? Inscrivez-vous.</a></strong>
</form>
{% endblock %}
