<h1> Joe Smack Symfony </h1>

<div> You received this mail because you submit a reset password request on 
    <a href="https://joe-smack-symfony.herokuapp.com/">https://joe-smack-symfony.herokuapp.com/</a>
</div>

<p>To reset your password, please visit the following link</p>

<a href="{{ url('reset_password', {token: resetToken.token}) }}">{{ url('reset_password', {token: resetToken.token}) }}</a>

<p>This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>
